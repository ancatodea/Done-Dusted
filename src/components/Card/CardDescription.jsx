export default function CardDescription(props) {
    return (
        <>
            <p>{props.cardDescription}</p>
            <p>If done/todo no add button</p>
            <p>If no favourite add favorite button</p>
            <p>Add a card for every task</p>
        </>
    );
}
